import React from 'react';

type BangumiInfo = {
    title: string,
    title_japanese: string,
    synopsis: string,
    rating: number,
    image: string,
}

const bangumiInfo = (props : BangumiInfo) => {

}
